/*
 * Filename: StyleBroken.ts
 * FullPath: modules/projects/dom.ts/src/mixin/StyleBroken.ts
 * Reason for changes: Keep the historical import path as a compatibility
 * facade over the canonical runtime style bridge.
 */

export * from "./Style";
